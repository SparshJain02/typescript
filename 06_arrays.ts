const arr: number[] = [];
// this will create empty array which takes only numbers
arr.push(2);
arr.push(3);

const brr: Array<number>=[]
// this is the same just slightly different syntax we can perform the same with this also
brr.push(2);
brr.push(3);


// this is array of array we can insert values manually also no need to push if you can insert manually
const matrix: Array<Array<number>> = [
    [255,255,255],
    [1,2,3],
    [1,2]
]
// const matrix: number[][] = [
//     [255,255,255],
//     [1,2,3],
//     [1,2]
// ]

export{}